@page
@model ErrorModel
@{
    ViewData["Title"] = "Erro";
}

<h1 class="text-danger">Erro.</h1>
<h2 class="text-danger">Ocorreu um erro no processamento da solicitação.</h2>

@if (Model.ShowRequestId)
{
    <p>
        <strong>Solicitação:</strong> <code>@Model.RequestId</code>
    </p>
}